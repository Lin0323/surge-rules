#!name=YouTube 去广告（iOS 15+）
#!desc=独立模块：过滤首页瀑布流和播放页推荐列表广告；不保证移除视频贴片广告。首次启用前须启用 HTTPS 解密并信任 Surge 证书；不修改主配置。
#!system=ios

# 采用 app2smile/rules 当前维护的 iOS YouTube 模块逻辑。
# 仅解析 youtubei.googleapis.com 的应用 API，不修改 DNS、代理策略或其他应用规则。

[MITM]
hostname = %APPEND% youtubei.googleapis.com

[Script]
youtube-proto = type=http-response,pattern=^https:\/\/youtubei\.googleapis\.com\/youtubei\/v1\/(browse|next),requires-body=1,binary-body-mode=1,max-size=2097152,script-path=https://raw.githubusercontent.com/app2smile/rules/master/js/youtube.js,script-update-interval=86400
